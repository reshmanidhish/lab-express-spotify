{{#each trackdetails}}
<h2>{{name}}</h2>
<audio
        controls
        src={{this.preview_url}}>
            <a href={{this.preview_url}}>
                Download audio
            </a>
    </audio>
{{/each}}